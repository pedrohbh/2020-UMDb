
# Configuracoes gerais:
spring.datasource.username=root
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5Dialect
spring.servlet.multipart.max-file-size=200MB
spring.servlet.multipart.max-request-size=200MB
spring.data.rest.base-path=/api
server.servlet.session.timeout=300m
security.basic.enabled=false

# Configs que diferem na producao e no desenvolvimento
spring.datasource.url=jdbc:mysql://localhost:3306/umdb?useSSL=false&useTimezone=true&serverTimezone=UTC 
#spring.datasource.url=jdbc:mysql://mysql-docker-container:3306/umdb?useTimezone=true&serverTimezone=UTC
spring.datasource.password=senha123
server.port=8080
tipo.operacao=dev


################################
#server.servlet.contextPath=/ndb
server.servlet.contextPath=/umdb
server.use-forwarded-headers=true
################################


# Configuracoes das datas:
spring.jackson.date-format=yyyy-MM-dd
spring.jackson.time-zone=Brazil/East

# Configuracoes do CORS
#management.endpoints.web.cors.allow-credentials=* # Whether credentials are supported. When not set, credentials are not supported.
management.endpoints.web.cors.allowed-headers=* # Comma-separated list of headers to allow in a request. '*' allows all headers.
management.endpoints.web.cors.allowed-methods=* # Comma-separated list of methods to allow. '*' allows all methods. When not set, defaults to GET.
management.endpoints.web.cors.allowed-origins=* # Comma-separated list of origins to allow. '*' allows all origins. When not set, CORS support is disabled.
management.endpoints.web.cors.exposed-headers=* # Comma-separated list of headers to include in a response.
management.endpoints.web.cors.max-age=3600s
